import { onUnmounted } from "vue";
import manager from './hotkeyManager';
export default (function (hotKeys) {
    hotKeys.forEach(function (hotkey) { return manager.registerHotKey(hotkey); });
    onUnmounted(function () {
        hotKeys.forEach(function (hk) { return manager.removeHotKey(hk); });
    });
    return function () {
        hotKeys.map(function (hk) { return function () {
            manager.removeHotKey(hk);
        }; });
    };
});
//# sourceMappingURL=useHotKey.js.map